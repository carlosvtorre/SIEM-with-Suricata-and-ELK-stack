# Linux high speed capture support
af-packet:
  - interface: eth0 (change it with your own)
    # Number of receive threads. "auto" uses the number of cores
    #threads: auto
    # Default clusterid. AF_PACKET will load balance packets based on flow.
    cluster-id: 99

#Live rule Reloading 

detect-engine:
  - rule-reload: true
